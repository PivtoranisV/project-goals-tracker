import React from 'react';
import Header from '../Header/Header';

const Login = (props) => {
  return (
    <React.Fragment>
      <Header />
      <div>Login</div>
    </React.Fragment>
  );
};

export default Login;
